import { DefineWorkflow, Schema } from "deno-slack-sdk/mod.ts";
import { EmailListenerFunction } from "../functions/email_listener_function.ts";

// Add the definition below

// Add the handler below

export default EmailWorkflow;
